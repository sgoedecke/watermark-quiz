[project]
name = "watermark-quiz"
version = "0.1.0"
description = "Generate blind SynthID text-watermark quizzes"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.2",
    "safetensors>=0.4",
    "torch>=2.6",
    "transformers>=4.51",
]

[tool.uv]
package = false

